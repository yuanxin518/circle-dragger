export type UniversalAttrType = {
  position: {
    name: "组件位置";
    offsetLeft: number;
    offsetTop: number;
  };
  size: {
    name: "组件尺寸";
    width: number;
    height: number;
  };
};
